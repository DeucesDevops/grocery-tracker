apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: grocery-tracker
  labels:
    app: api
    app.kubernetes.io/part-of: grocery-tracker
spec:
  type: ClusterIP
  selector:
    app: api
  ports:
    - name: http
      port: 5001
      targetPort: 5001
      protocol: TCP
