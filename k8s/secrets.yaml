# NOTE: This file contains placeholder values only.
# NEVER commit real secrets to git.
#
# In production, use one of:
#   - AWS Secrets Manager + External Secrets Operator
#   - Sealed Secrets (bitnami/sealed-secrets)
#   - HashiCorp Vault with Vault Agent Injector
#
# To create the secret manually:
#   kubectl create secret generic grocery-tracker-secrets \
#     --namespace grocery-tracker \
#     --from-literal=database-url='postgresql://user:pass@postgres:5432/grocerytracker' \
#     --from-literal=redis-url='redis://:pass@redis:6379' \
#     --from-literal=redis-password='CHANGE_ME' \
#     --from-literal=jwt-secret='CHANGE_ME_32_CHAR_RANDOM_STRING' \
#     --from-literal=postgres-db='grocerytracker' \
#     --from-literal=postgres-user='groceryuser' \
#     --from-literal=postgres-password='CHANGE_ME'

apiVersion: v1
kind: Secret
metadata:
  name: grocery-tracker-secrets
  namespace: grocery-tracker
  labels:
    app.kubernetes.io/part-of: grocery-tracker
type: Opaque
stringData:
  database-url: "postgresql://CHANGE_ME:CHANGE_ME@postgres:5432/grocerytracker"
  redis-url: "redis://:CHANGE_ME@redis:6379"
  redis-password: "CHANGE_ME"
  jwt-secret: "CHANGE_ME_32_CHAR_RANDOM_STRING"
  postgres-db: "grocerytracker"
  postgres-user: "CHANGE_ME"
  postgres-password: "CHANGE_ME"
